(defpackage spinor.simulator-test
  (:use :cl
        :spinor.simulator
        :prove))
(in-package :spinor.simulator-test)

;; NOTE: To run this test file, execute `(asdf:test-system :spinor.simulator)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
